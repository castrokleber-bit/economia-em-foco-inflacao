# Camada de dados e backend
httpx>=0.27
fastapi>=0.110
uvicorn[standard]>=0.29

# Leitura por IA (opcional — só o rascunho de leitura para a indústria)
anthropic>=0.40

# Testes / gates de validação
pytest>=8.0
